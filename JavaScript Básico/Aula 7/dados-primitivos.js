/// dados primitivos: String, number, underfined, null, boolean

const nome = 'joão'; // string
const nome1 = "joão"; // string
const nome2 = `joão`; // string
const num1 = 10; // number
const num2 = 10.9 /// number
let nomeAluno; /// Underfined -> não aponta pra nenhum local na memória
const sobrenome = null; // Nulo -> não aponta pra nenhum local
const aprovado = false; // Boolean = true, false (lógico)